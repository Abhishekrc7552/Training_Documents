Docker Commands :::::

docker images                                                                                           ## List the Images.
docker ps                                                                                               ## List the Active/running Containers.
docker ps -a                                                                                            ## List all the Container (Active & In-Active).

docker pull ubuntu(Image name)
docker pull tomcat:8.0
      (Image name : Version/Tag name)

docker run <image_name>		              	                                                              ## To create a Container based on Container Image.
docker run ubuntu sleep 20                                                                              ## Run docker in foreground mode.
docker run -d ubuntu sleep 20                                                                           ## Run docker in background mode.
docker run -it ubuntu bash                                                                              ## Run docker in Interactive mode.

docker start <Container_ID>
docker exec -it <Container_ID> bash
docker stop <Container_ID>

docker run -it -p 8085:8080 tomcat:8.0    #                                                             ## Mapping or Port binding.
        <host_VM_port>:<Container_port> container_image_name

docker rmi tomcat:8.0                                                                                   ## Remove Image.
docker rm <Container_ID>                                                                                ## Remove Container using Container_ID.


docker volume list                                                                                      ## Lista all the volumes created.
docker volume create test-vol1                                                                          ## Create Volume test-vol1
docker volume inspect test-vol1                                                                         ## Detailed info of Volume.
docker run -it --mount source=test-vol1,destination=/test-vol1 ubuntu bash                              ## mounting a source path thats linked with Volume.

docker commit 8a2a4e7375f1 abhisheketa/mvn-img:v1.0                                                     ## Create a new Container image based on existing Container.
docker commit <existing Con ID> <dockerhub_repo_name>/<New_image_name>:<Tag_name>



vi Dockerfile                                                                                           ## Pre-requiste for the Docker build command.

FROM ubuntu
RUN apt update -y
RUN apt install git -y
RUN apt install maven -y



docker build -t abhisheketa/mvn-self-img:v1.0 .                                                         ## Create a new Container with help of a Dockerfile which contains required installation commands.
docker build -t <dockerhub_repo_name>/<New_image_name>:<Tag_name> .                                     ## " . " is used to direct the current directory of Dockerfile.


## Personal Access Token :::

docker login -u abhishekrc7552

dckr_pat_WChwTJ3y8wwR8d-dzYLM-9gyih0



